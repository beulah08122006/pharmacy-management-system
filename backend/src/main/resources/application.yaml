spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pharmacy_db
    username: root
    password: beulah0812

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  sql:
    init:
      mode: never

server:
  port: 5050

stripe:
  secret-key: ${STRIPE_SECRET_KEY}